{
  "mcpServers": {
    "payload-production": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://thechief-clean.vercel.app/api/plugin/mcp",
        "--header",
        "Authorization: Bearer ${MCP_API_KEY}"
      ],
      "env": {
        "MCP_API_KEY": "thechief-mcp-secret-key-2024"
      }
    }
  }
}